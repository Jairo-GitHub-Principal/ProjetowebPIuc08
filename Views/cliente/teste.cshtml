 @using Microsoft.AspNetCore.Http;
 @if(Context.Session.GetInt32("Id")==null){// admin não logado
            

             @if(Context.Session.GetInt32("idCliente")==null){ // cliente não logado
               
             }
             else{ // cliente logado 

             }
    } else{//ADM logado

                   

            }